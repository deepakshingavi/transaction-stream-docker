```
//  Clone 
$ git clone git@github.com:deepakshingavi/transaction-stream-docker.git
$ cd transaction-stream-docker
// build docker image using SBT 
$ sbt docker
// Start the docker image for Kafka Produer and Conusmer
$ docker-compose up
```
